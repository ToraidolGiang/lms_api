server.port=${PORT:8080}

# PostgreSQL
spring.datasource.url=${URL_DB}
spring.datasource.username=${USERNAME_DB}
spring.datasource.password=${PASSWORD_DB}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MongoDB


# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000
jwt.refresh-expiration=604800000

#PayOS
PAYOS_CLIENT_ID=${PAYOS_CLIENT_ID}
PAYOS_API_KEY=${PAYOS_API_KEY}
PAYOS_CHECKSUM_KEY=${PAYOS_CHECKSUM_KEY}

#Cloudinary
cloudinary.cloud-name=dueoalej1
cloudinary.api-key=854542144758543
cloudinary.api-secret=fNDmQnPAiF25vxGhdxB1J1UQ5NE
payos.webhook.verify-signature=${PAYOS_VERIFY_SIGNATURE:true}

# Ép Spring tìm các biến từ file .env của hệ thống
MONGO_URI=${MONGO_URI}
MONGO_DATABASE=${MONGO_DATABASE}
